1.By searching the function name “strcmp” in the assembly text, what instruction calls strcmp?

  (strcmp(input, password)) calls the function strcmp in the assembly text
JYM: ok that is the C statement. but what instr actually calls strcmp?
2.Where is the password “secret” stored?

  The password "secret" is stored in password above the main method. This saves the chars as hex.
